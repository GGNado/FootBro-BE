name: Deploy Backend Only

on:
  push:
    branches: [main]

jobs:
  deploy-backend:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up JDK 24
        uses: actions/setup-java@v3
        with:
          java-version: '24'
          distribution: 'zulu'

      - name: Build with Maven
        run: ./mvnw clean package -DskipTests

      - name: Build Docker Image
        run: docker build -t footbro-backend:latest .

      - name: Save Docker Image
        run: docker save footbro-backend:latest | gzip > backend.tar.gz

      - name: Copy Image to VPS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: root
          key: ${{ secrets.DEPLOY_KEY }}
          source: "backend.tar.gz"
          target: "/tmp"

      - name: Deploy Backend Container
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.VPS_HOST }}
          username: root
          key: ${{ secrets.DEPLOY_KEY }}
          script: |
            # Load new image
            cd /tmp && docker load < backend.tar.gz
            
            # Stop and remove old backend container
            docker stop footbro-backend 2>/dev/null || true
            docker rm footbro-backend 2>/dev/null || true
            
            # Start only backend service
            cd /root/footbro
            docker compose up -d --no-deps backend
            
            # Cleanup
            rm -f /tmp/backend.tar.gz